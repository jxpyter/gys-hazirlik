# Netlify Configuration for Next.js 16

[build]
  command = "npm run build"
  # Don't set publish - the Next.js plugin handles it

[build.environment]
  NODE_VERSION = "20"

# Enable Next.js runtime
[[plugins]]
  package = "@netlify/plugin-nextjs"
